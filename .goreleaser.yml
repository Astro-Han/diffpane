version: 2

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s
      - -w

checksum:
  name_template: checksums.txt

release:
  github:
    owner: Astro-Han
    name: diffpane
